export enum BetResult {
  WIN = 'WIN',
  LOSE = 'LOSE',
  DRAW = 'DRAW',
  NOT_FINISHED = 'NOT_FINISHED',
}

export enum PaymentAction {
  BET = 'BET',
  RESULT = 'RESULT',
  DEPOSIT = 'DEPOSIT',
  WITHDRAW = 'WITHDRAW',
}

export enum BetType {
  SINGLE = 'SINGLE',
  MULTI = 'MULTI',
  ACCUMULATOR = 'ACCUMULATOR',
}
