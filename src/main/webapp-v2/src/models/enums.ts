export enum BetResult {
  WIN = 'WIN',
  LOSE = 'LOSE',
  DRAW = 'DRAW',
  PENDING = 'PENDING',
  CANCELLED = 'CANCELLED'
}

export enum BetType {
  HANDICAP = 'HANDICAP',
  OVER_UNDER = 'OVER_UNDER',
  MONEY_LINE = 'MONEY_LINE'
}

export enum PaymentAction {
  DEPOSIT = 'DEPOSIT',
  WITHDRAW = 'WITHDRAW',
  BET = 'BET',
  WIN = 'WIN',
  LOSE = 'LOSE'
}
